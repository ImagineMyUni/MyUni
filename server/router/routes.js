
// Global
const HOME = "/";
const LOGIN = "/login";
const JOIN = "/join";

// Users
const USER = "/user";
// API
const API = "/api";


const routes = {
    home: HOME,
    login: LOGIN,
    join: JOIN,

    user: USER,
    
    api: API
};

export default routes; 

